const heros: string[] = [];

const yourNumber: number[] = [];

type User = {
    name: string,
    isActive: boolean,
}
const allUser: User[] = []; 

const MLModels: number[][] = [
    [255, 255, 255],
    []
];

heros.push("spider man");
yourNumber.push(123);
allUser.push({ name: "tien", isActive: false });
